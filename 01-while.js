// STEP 1: How to link this file to the html
console.log("INDEX JS IS WORKING");

// WHILE

let index = 0;

/* while(index < 99){ // if this is false, it never runs the code
    console.log(`The number of the index is: ${Math.random()}`);

    index++
} */

// DO WHILE

do { // it allways runs once
    console.log("Hello do while ");
} while (index !== 0 ); // if this is false is not running again

